---
name: launch-triage
description: Разбор упавшего прогона Allure TestOps — группировка падений по корневой причине с контекстом Redmine-задачи запуска и сводкой в чат. Use when пользователь просит разобрать упавший прогон, понять почему упал запуск, сгруппировать падения, сделать триаж launch — даже если не говорит слово «триаж».
---

# Разбор упавшего прогона

Ты разбираешь упавший запуск Allure TestOps: берёшь статистику, находишь задачу запуска, раздаёшь чтение результатов суб-агенту `failure-analyst` и сводишь всё в одну сводку. Алгоритм чтения одного результата, правила нормализации сигнатур и типы причин живут в `agents/failure-analyst.md` — агент читает их сам, **не пересказывай их в промпте агента**.

## Вход

- Число (`4498`) или ссылка `https://astbroker.qatools.cloud/launch/<id>` → id запуска.
- `projectId` — обязателен: пользователь не задал явно — спроси, дефолта нет. Ориентир: прогоны автотестов в проекте 37, ручные прогоны в проекте 1.

## Критические правила

1. **Read-only.** Ни кейсы, ни дефекты, ни задачи не создаём и не меняем. Файлы не пишем — сводка идёт в чат.
2. **Делегирование обязательно.** Результаты прогона приходят вместе с `trace`; читать их в главном треде нельзя — контекст взорвётся. Любое чтение результатов и задачи — через суб-агента `failure-analyst` (**Agent tool**).
3. **Задачу тянем один раз** — режимом `context`, и передаём выжимку всем `drill`-вызовам. Агенты Redmine сами не дёргают.
4. **Лимиты:** до 8 агентов `scan` параллельно, до 12 агентов `drill` на прогон.

## Шаг 1. Статистика запуска

`testops_find_launches`:

````
projectId: <projectId>
aql: "id = <launch>"
expand: ["statistic","issues","release","category","defect","unresolved","uncategorized"]
````

Из ответа возьми: имя запуска, `release`, `issues`, `statistic` (по статусам), и сколько падений TestOps уже разложил по дефектам (`defect` / `uncategorized`).

- Пустой результат (`totalElements: 0`) → запуск в другом проекте: спроси у пользователя `projectId`, не перебирай проекты сам.
- `failed` + `broken` = 0 → ответь «прогон зелёный, падений нет» и остановись.

## Шаг 2. Номер Redmine-задачи

Порядок источников:
1. `issues[0]` — MCP отдаёт массивом строк (`["43203"]`). Строка из одних цифр → это номер задачи Redmine.
2. Иначе `release` — имя релиза часто и есть номер задачи (`"43203"`).
3. Ни то ни другое (или значение не число) → работаем без контекста, в сводке строка «задача не определена, анализ только по ошибкам». У прогонов автотестов `issues` и релиз обычно пустые — это штатно, разбор продолжается по ошибкам.

## Шаг 3. Раздача агентов

Параллельно, одним блоком вызовов:
- один `failure-analyst` с `mode: context`, `issue_id` — если номер нашёлся;
- `failure-analyst` с `mode: scan` на каждую страницу: страниц = min(ceil((failed + broken) / 50), 8) — одна волна, без дозапроса лишних страниц; вход каждому: `projectId`, `launch`, `page`, `size: 50`.

Страниц вышло бы больше 8 (падений больше 400) → в сводке отметь, сколько результатов осталось непросмотренными за вычетом первых 8 страниц.

## Шаг 4. Сверка охвата и мерж групп

Сложи «прочитано N» из заголовков всех `scan`-ответов (страницы с `ОШИБКА` и пустые страницы дают 0) и сравни с `failed + broken` из шага 1.
- Сумма меньше → в шапке сводки строка «прочитано N из M падений».
- Все страницы пустые, а `failed + broken` > 0 → сводку не строй: сообщи пользователю, что фильтр результатов не сработал (вероятно, неверное AQL-поле запуска), и останови разбор.

Дальше мержи строки `SIG:`:
- одинаковая сигнатура из разных страниц — одна группа, счётчики складываются, **статус игнорируем** (одна и та же ошибка может приходить и `failed`, и `broken`) — в таблице сводки колонки статуса нет;
- `id` группы берём до 6 (по возможности с разных страниц) — все идут в `result_ids` для `drill`, первые 1–3 — в колонку «Примеры» сводки вместе с именем теста из поля `test=`;
- строки `SIG: <прочее> | mixed` с обычными сигнатурами не мержатся — их `N` суммируются отдельно, в блок «прочее»; `drill` по «прочее» не запускаем.

Отсортируй группы по убыванию количества. Группы с 1 падением и непохожими сигнатурами тоже уходят в блок «прочее» — безусловно, независимо от того, сколько групп уже набралось.

Топ-14 на странице (обрезка в `scan`) занижает счётчики хвоста — в сводке блок «прочее» помечай как оценку (`~N`).

## Шаг 5. Разбор групп

На каждую группу (максимум 12, начиная с самых крупных) — `failure-analyst` с `mode: drill`, входом `projectId`, `launch`, `signature`, `count`, `result_ids`, `issue_context` (выжимка из шага 3; нет задачи — пустая строка). `result_ids` — до 6 id группы из шага 4. Подгруппы в сводке идут отдельными строками таблицы с общим номером группы (`3a`, `3b`); их «Падений» — оценка по выборке, пиши с `~`.

## Формат сводки

```
Launch 5034 «Запуск от 18/09/2026 18:13» · релиз 43203 · Redmine #43203 — <тема задачи>
773 теста: 469 passed, 56 failed, 240 broken, 8 skipped
TestOps уже разложил по дефектам: 12 падений (осталось разобрать 284)

| # | Тип                    | Причина                                   | Падений | Примеры |
|---|------------------------|--------------------------------------------|---------|---------|
| 1 | инфра/авторизация      | 401 на справочниках при старте теста       | 240     | 39979   |
| 2 | продуктовый дефект     | HTTP 500 на /contracts/calculate (ВСК)     | 12      | 40012   |
| 3 | тестовые данные/окружение | ВЕСТА GFK350 не привязана к СК          | 9       | 40031   |

Детали по группам — причина, где ломается, что чинить.

### 1. 401 на справочниках — инфра/авторизация
Причина: ...
Где: ...
Что чинить: ...
```

Хвостом сводки — блок «Что дальше»: для устойчивых групп предложи закрепить их дефектом TestOps (`testops_create_defect` с `matcher.messageRegex`), чтобы следующий прогон раскладывался автоматически. **Предлагай, не выполняй.**

## Краевые случаи

- **Запуск не найден** → спросить `projectId`, не перебирать проекты.
- **Падений нет** → «прогон зелёный», стоп.
- **Redmine недоступен** (агент `context` вернул «Задача недоступна») → разбор продолжаем без контекста: в шапке сводки строка «контекст задачи недоступен: <причина>», в `drill` передаём пустой `issue_context` (не текст ошибки).
- **Фильтр `scan` не сработал** (все страницы пустые при `failed + broken` > 0) → см. шаг 4: сообщить и остановиться, сводку не строить.
- **Агент `scan` упал на своей странице** → остальные страницы идут дальше; в сводке строка «страница N не прочитана, результаты не учтены» (страница не входит в «прочитано»).
- **Агент `drill` вернул `ОШИБКА`** → группа остаётся в таблице со своим счётчиком, тип «не разобрано», причина — текст ошибки; блока деталей у неё нет.
- **Все падения уже разложены дефектами TestOps** → это не отдельная ветка: в шапке сводки строка «N падений уже покрыты дефектами TestOps», дальше — обычный разбор (шаги 3–5).
